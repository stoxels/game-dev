# === Stoxels - only this is needed for the game ===
# animations/, audio/, css/, images/, js/, index.html, main.js
# Everything below is local/dev-only and will NOT be pushed.

.freebuff/
.vs/
turnaround_images/
walk_videos/
walk_frames/
tools/

# local preview server / temp
*.pid
.preview_server.pid

# Word lock files like ~$rite-Walking-Prompt (2) (2).docx
~$*
*~
*.bak

# OS junk
.DS_Store
Thumbs.db

# dev notes, not needed in game
turnaround_checklist.md
