# ============================================================================
# PUSH-WHITELIST for GitHub Pages (inverted logic: everything is ignored
# EXCEPT what the published game needs). GitHub Pages serves the repo as-is,
# so only true game files may be pushed - nothing else.
#
# The game online needs exactly:
#   index.html, main.js, animations/, audio/, css/, images/, js/,
#   generated/phase2/entry.mjs + order.json + manifest-imports.json
#   (index.html loads generated/phase2/entry.mjs), .nojekyll (stops GitHub's
#   site builder from skipping underscore files like animations/Stox/abilities/_shared)
#
# Everything else (tools, tests, docs, notes, art sources, previews) stays
# local-only and is never committed.
# ============================================================================

# --- ignore everything, then allow the game back in ---
/*

# --- the published game ---
!/.gitattributes
!/.gitignore
!/.nojekyll
!/index.html
!/main.js
!/animations/
!/audio/
!/css/
!/images/
!/js/
!/generated/

# --- generated/: only the three module files the page loads ---
/generated/*
!/generated/phase2/
/generated/phase2/*
!/generated/phase2/entry.mjs
!/generated/phase2/order.json
!/generated/phase2/manifest-imports.json

# --- junk that must never be pushed even from allowed folders ---
node_modules/
dist/
**/~$*
*.pid
.preview_server.pid
.eslintcache
.DS_Store
Thumbs.db
**/.vs/
**/.vscode/
